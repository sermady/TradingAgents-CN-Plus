# 实时行情功能说明文档

## 📋 概述

为了解决"盘中分析必须使用实时价格"的需求，系统现已集成**实时行情功能**。该功能可以自动判断当前是否为交易时间，并在盘中交易时段优先使用实时行情数据，确保分析使用的是最新价格。

## 🎯 核心功能

### 1. 交易时间智能判断

系统可以自动判断以下市场的交易时间：

#### A股市场
- **上午交易时段**: 9:30 - 11:30
- **下午交易时段**: 13:00 - 15:00
- **时区**: Asia/Shanghai (UTC+8)
- **交易日**: 周一至周五（不含节假日）

#### 港股市场
- **上午交易时段**: 9:30 - 12:00
- **下午交易时段**: 13:00 - 16:00
- **时区**: Asia/Hong_Kong (UTC+8)
- **交易日**: 周一至周五（不含节假日）

#### 美股市场
- **常规交易时段**: 9:30 - 16:00 (EST/EDT)
- **盘前交易**: 4:00 - 9:30 (EST/EDT)
- **盘后交易**: 16:00 - 20:00 (EST/EDT)
- **时区**: America/New_York
- **交易日**: 周一至周五（不含节假日）

### 2. 实时行情数据源

系统支持以下实时行情数据源（按优先级排序）：

1. **MongoDB缓存** (最高优先级)
   - 来源: `market_quotes` 集合
   - 优势: 速度最快，已经过数据清洗
   - 更新频率: 实时/准实时

2. **AKShare实时行情**
   - 接口: `stock_zh_a_spot_em()`
   - 支持: A股实时行情
   - 优势: 免费，稳定

3. **Tushare实时行情**
   - 需要: 高级权限
   - 支持: A股实时行情
   - 状态: 暂未完全启用

### 3. 数据自动切换策略

```
┌─────────────────────┐
│   发起股票分析请求   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 判断当前是否交易时间 │
└──────────┬──────────┘
           │
           ├───── 是 ────▶ ┌──────────────────┐
           │               │  获取实时行情     │
           │               │  (10秒缓存)      │
           │               └────────┬─────────┘
           │                        │
           │                        ▼
           │               ┌──────────────────┐
           │               │ 合并实时行情到   │
           │               │ 历史数据结果     │
           │               └────────┬─────────┘
           │                        │
           └───── 否 ────▶ ┌────────▼─────────┐
                           │  使用历史数据     │
                           │  (1小时缓存)     │
                           └──────────────────┘
```

## 💡 使用方法

### 自动模式（推荐）

系统会自动判断并使用实时行情，**无需任何代码修改**：

```python
from tradingagents.dataflows.data_source_manager import get_data_source_manager

manager = get_data_source_manager()

# 系统会自动判断是否是交易时间
# 如果是盘中，会自动使用实时行情
result = manager.get_stock_data("600765", "2025-01-19", "2025-01-19")
```

### 手动获取实时行情

如果需要单独获取实时行情：

```python
from tradingagents.dataflows.data_source_manager import get_data_source_manager

manager = get_data_source_manager()

# 直接获取实时行情
quote = manager.get_realtime_quote("600765")

if quote:
    print(f"价格: ¥{quote['price']:.2f}")
    print(f"涨跌幅: {quote['change_pct']:+.2f}%")
    print(f"时间: {quote['date']} {quote['time']}")
```

### 交易时间判断

手动判断是否是交易时间：

```python
from tradingagents.utils.market_time import MarketTimeUtils

# 判断是否是A股交易时间
is_trading, status = MarketTimeUtils.is_a_stock_trading_time()
print(f"是否交易中: {is_trading}")
print(f"状态: {status}")

# 获取完整的市场状态
market_status = MarketTimeUtils.get_market_status("600765")
print(market_status)
```

## 📊 数据格式

### 实时行情数据结构

```python
{
    "symbol": "600765",           # 股票代码
    "price": 19.15,               # 最新价格
    "open": 19.50,                # 今开
    "high": 19.80,                # 最高
    "low": 19.00,                 # 最低
    "volume": 12500000,           # 成交量
    "amount": 240000000,          # 成交额
    "change": -0.35,              # 涨跌额
    "change_pct": -1.79,          # 涨跌幅(%)
    "date": "2025-01-19",         # 日期
    "time": "14:30:15",           # 时间
    "source": "akshare_realtime", # 数据源
    "is_realtime": True           # 是否实时数据
}
```

### 集成实时行情的报告格式

盘中交易时段，分析报告会显示：

```markdown
📊 中航重机(600765) - 技术分析数据
数据期间: 2025-01-09 至 2025-01-19

⚡ 实时行情（盘中）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💰 实时价格: ¥19.15
📈 涨跌: -0.35 (-1.79%)
📊 今开: ¥19.50  |  最高: ¥19.80  |  最低: ¥19.00
🕐 更新时间: 2025-01-19 14:30:15
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

最新数据日期: 2025-01-19
数据条数: 30条 (展示最近5个交易日)

📊 移动平均线 (MA):
   MA5:  ¥19.86 (价格在MA5下方 ↓)
   ...
```

## ⚙️ 配置说明

### 缓存策略

系统根据交易状态自动调整缓存时间：

| 市场状态 | 缓存时长 | 说明 |
|---------|---------|------|
| 盘中交易 | 10秒 | 快速更新，保证实时性 |
| 盘后/非交易日 | 1小时 | 延长缓存，减少API调用 |

可以通过以下函数获取缓存超时时间：

```python
from tradingagents.utils.market_time import get_realtime_cache_timeout

timeout = get_realtime_cache_timeout("600765")
print(f"缓存超时: {timeout}秒")
```

### 环境变量

无需额外配置环境变量，系统会自动使用现有的数据源配置。

## 🔧 技术实现

### 核心文件

1. **`tradingagents/utils/market_time.py`**
   - 交易时间判断逻辑
   - 市场状态查询
   - 缓存策略计算

2. **`tradingagents/dataflows/data_source_manager.py`**
   - 实时行情获取方法
   - 数据源切换逻辑
   - 实时行情与历史数据合并

3. **`tradingagents/dataflows/cache/app_adapter.py`**
   - MongoDB实时行情缓存读取

### 核心类和方法

```python
# 交易时间判断
class MarketTimeUtils:
    @staticmethod
    def is_a_stock_trading_time(check_time=None) -> Tuple[bool, str]
    
    @staticmethod
    def is_hk_stock_trading_time(check_time=None) -> Tuple[bool, str]
    
    @staticmethod
    def is_us_stock_trading_time(check_time=None, include_extended=False) -> Tuple[bool, str]
    
    @staticmethod
    def should_use_realtime_quote(symbol, check_time=None) -> Tuple[bool, str]
    
    @staticmethod
    def get_market_status(symbol, check_time=None) -> Dict

# 实时行情获取
class DataSourceManager:
    def get_realtime_quote(self, symbol: str) -> Optional[Dict]
    
    def _get_akshare_realtime_quote(self, symbol: str) -> Optional[Dict]
    
    def _merge_realtime_quote_to_result(self, historical_result: str, 
                                       realtime_quote: Dict, 
                                       symbol: str) -> str
```

## 🧪 测试验证

运行测试脚本验证功能：

```bash
# 测试实时行情功能
python scripts/test_realtime_quote.py

# 测试数据日期标注功能
python scripts/test_data_date_fix.py
```

### 测试用例

测试脚本会验证：

1. ✅ 交易时间判断准确性
2. ✅ 实时行情数据获取
3. ✅ 实时行情与历史数据集成
4. ✅ 缓存超时策略
5. ✅ 多市场支持（A股/港股/美股）

## 📝 使用示例

### 示例1: 盘中分析

```python
from tradingagents.graph.trading_graph import TradingAgentsGraph
from tradingagents.default_config import DEFAULT_CONFIG

# 初始化
config = DEFAULT_CONFIG.copy()
ta = TradingAgentsGraph(debug=True, config=config)

# 在交易时间内执行分析，会自动使用实时行情
_, decision = ta.propagate("600765", "2025-01-19")
print(decision)
```

### 示例2: 检查市场状态

```python
from tradingagents.utils.market_time import MarketTimeUtils

# 检查A股市场状态
status = MarketTimeUtils.get_market_status("600765")

print(f"股票: {status['symbol']}")
print(f"市场: {status['market']}")
print(f"当前时间: {status['current_time']}")
print(f"市场状态: {status['status']}")
print(f"是否交易中: {status['is_trading']}")
print(f"是否使用实时行情: {status['should_use_realtime']}")
print(f"原因: {status['reason']}")
```

### 示例3: 对比实时与历史

```python
from datetime import datetime, timedelta
from tradingagents.dataflows.data_source_manager import get_data_source_manager

manager = get_data_source_manager()
symbol = "600765"

# 获取实时行情
realtime = manager.get_realtime_quote(symbol)
if realtime:
    print(f"实时价格: ¥{realtime['price']:.2f}")
    print(f"更新时间: {realtime['date']} {realtime['time']}")

# 获取完整数据（会自动集成实时行情）
end_date = datetime.now().strftime("%Y-%m-%d")
start_date = (datetime.now() - timedelta(days=10)).strftime("%Y-%m-%d")
result = manager.get_stock_data(symbol, start_date, end_date)
print(result[:500])  # 查看前500字符
```

## ⚠️ 注意事项

### 1. 数据源限制

- **AKShare**: 免费但可能有频率限制
- **Tushare**: 实时行情需要高级权限（积分要求）
- **MongoDB**: 需要配置`market_quotes`集合的实时更新

### 2. 交易时间判断

- 目前不考虑法定节假日（如春节、国庆等）
- 仅判断周一至周五的常规交易时间
- 特殊情况（如临时停市）不在判断范围内

### 3. 数据延迟

- 实时行情可能有1-3秒延迟
- MongoDB缓存数据取决于更新频率
- 网络延迟会影响数据获取速度

### 4. 盘后处理

- 盘后时间使用历史数据（收盘价）
- 不显示"实时行情"标识
- 仍会标注最新数据日期

## 🎯 效果展示

### 盘中分析（使用实时行情）

```
📊 中航重机(600765) - 技术分析数据
数据期间: 2025-01-09 至 2025-01-19

⚡ 实时行情（盘中）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💰 实时价格: ¥19.15
📈 涨跌: -0.35 (-1.79%)
📊 今开: ¥19.50  |  最高: ¥19.80  |  最低: ¥19.00
🕐 更新时间: 2025-01-19 14:30:15
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

最新数据日期: 2025-01-19
数据条数: 30条 (展示最近5个交易日)

📊 移动平均线 (MA):
   MA5:  ¥19.86 (价格在MA5下方 ↓)
   MA10: ¥19.73 (价格在MA10下方 ↓)
   MA20: ¥18.97 (价格在MA20上方 ↑)
   MA60: ¥17.19 (价格在MA60上方 ↑)
```

### 盘后分析（使用历史数据）

```
📊 中航重机(600765) - 技术分析数据
数据期间: 2025-01-09 至 2025-01-19
最新数据日期: 2025-01-19
数据条数: 30条 (展示最近5个交易日)

💰 最新价格: ¥19.15 (数据日期: 2025-01-19)
📈 涨跌额: -0.35 (-1.79%)

📊 移动平均线 (MA):
   MA5:  ¥19.86 (价格在MA5下方 ↓)
   ...
```

## 🔄 升级说明

### 从旧版本升级

如果你使用的是旧版本，请按以下步骤升级：

1. **拉取最新代码**
```bash
git pull origin main
```

2. **安装新依赖**
```bash
pip install pytz
```

3. **无需修改现有代码**
   - 系统会自动启用实时行情功能
   - 现有的调用方式完全兼容

4. **可选：测试功能**
```bash
python scripts/test_realtime_quote.py
```

## 📚 相关文档

- [数据源配置文档](./data_sources.md)
- [缓存机制说明](./cache_mechanism.md)
- [API接口文档](./api_reference.md)

## 🤝 贡献

如需添加新的实时行情数据源或改进交易时间判断逻辑，请参考：

1. 在`market_time.py`中添加市场判断逻辑
2. 在`data_source_manager.py`中实现数据源接口
3. 添加相应的测试用例
4. 更新本文档

## 📞 支持

如有问题，请：

1. 查看测试脚本输出的错误信息
2. 检查日志文件`logs/tradingagents.log`
3. 提交Issue到项目仓库

---

**更新时间**: 2025-01-19  
**版本**: v1.0.0  
**状态**: ✅ 生产就绪